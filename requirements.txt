# ─── Web framework ───────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.38.0

# ─── Data engine ─────────────────────────────────────────────────
duckdb==1.4.4
pandas==2.3.2
pyarrow==23.0.0

# ─── File format support ─────────────────────────────────────────
openpyxl==3.1.5         # .xlsx read/write (OLE SMH files, exports)
beautifulsoup4==4.13.5  # HTML parsing for SMH HTML-disguised .xls
lxml==6.0.2             # BeautifulSoup parser

# ─── Utilities ───────────────────────────────────────────────────
python-dateutil==2.9.0.post0
setuptools==80.9.0
psutil==7.1.3           # heartbeat RSS in logs/ (optional — heartbeat degrades to uptime-only if absent)

# ─── OLE forecasting (predict endpoint) ──────────────────────────
statsmodels==0.14.6

# ─── Cycle Time module ───────────────────────────────────────────
requests==2.32.5        # IEDB3.0 HTTP client
python-dotenv==1.2.1    # .env loading for IEDB_CLIENT_KEY
